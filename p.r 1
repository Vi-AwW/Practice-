#include <stdio.h>

int main() {
  int t1, t2, t3;

  scanf("%d %d %d", &t1, &t2, &t3);

  float totalSpeed = 1.0 / t1 + 1.0 / t2 + 1.0 / t3;

  float time = 1.0 / totalSpeed;

  printf("%.2f", time);

  return 0;
}
